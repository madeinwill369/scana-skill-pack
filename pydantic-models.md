# Pydantic Model Patterns

**Category:** engineering

**Description:** None

## Instructions

Use Pydantic v2 models for all request/response schemas. Optional fields with None default. Use model_validator for cross-field validation. For JSONB fields use Dict[str, Any]. Always provide example values in Field() for API docs.

# Future-Proofing Patterns

**Category:** engineering

**Description:** None

## Instructions

Build for 100 tenants from day 1. Every query has tenant_id filter. Every table has created_at. Use JSONB for flexible data that might grow. Version your API (prefix /v1/ if planning breaking changes). Document every non-obvious design decision in memories.

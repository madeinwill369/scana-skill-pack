# Python Import Structure

**Category:** engineering

**Description:** None

## Instructions

FastAPI project structure: app/main.py (app factory + lifespan), app/routes/ (routers), app/db/ (connections), app/agents/ (agent logic), app/folk/ (pool logic). Always use relative imports within app. __init__.py in every directory.
